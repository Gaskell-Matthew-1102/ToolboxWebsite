# ToolboxWebsite
This repository contains the code for a descriptive webpage about the VEDB Toolbox project.

## How to run...
### Flask backend
Create a Python venv in server directory and install from requirements.txt.
Then execute `run.py`; the backend will be accessible at 0.0.0.0:8080.

### Vue frontend
In client directory, run `npm run dev`.
